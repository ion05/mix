# Mix

Native macOS 26 menu-bar extra for dual-device per-app volume and output.

Open `Mix.xcodeproj` in Xcode 26, sign with your Apple ID team, and run. Mix appears in the menu bar. It cannot live inside the system Sound tile.

Requires System Audio Recording the first time Mix taps an app.
